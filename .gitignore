__pycache__/
*.pyc
.pytest_cache/
plans/
manifests/
delivery/
work/
*.mp4
!samples/**/*.mp4
.env
# Service runtime state: the SQLite database, uploaded originals and rendered
# deliverables. Real customer files -- never in git, and the reason `cp` of this
# one directory is the entire backup procedure.
service/data/
